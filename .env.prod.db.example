# Copy to .env.prod.db and fill in values. Must match the matching
# SPORTSHOP_DB_* lines in ../backend/sportshop/.env.prod or the backend
# will fail to connect on first up.

POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
